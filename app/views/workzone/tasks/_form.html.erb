<%= form_with(model: [:workzone, workzone_task], local: true) do |form| %>
  <% if workzone_task.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(workzone_task.errors.count, "error") %> prohibited this task from being saved:</h2>

      <ul>
        <% workzone_task.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :title, style: "display: block" %>
    <%= form.text_field :title %>
  </div>

  <div>
    <%= form.label :description, style: "display: block" %>
    <%= form.text_area :description %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
